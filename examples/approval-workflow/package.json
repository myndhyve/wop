{
  "name": "@myndhyve/wop-example-approval-workflow",
  "version": "0.1.0",
  "private": true,
  "description": "WOP approval-gate example: start a workflow that suspends at an approval gate, resolve via POST /v1/runs/{runId}/approvals/{nodeId}, observe terminal completion. Requires a host claiming the wop-interrupts profile.",
  "type": "module",
  "scripts": {
    "start": "node approval-workflow.mjs",
    "test": "node approval-workflow.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
