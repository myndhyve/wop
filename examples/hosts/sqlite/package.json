{
  "name": "@myndhyve/wop-host-sqlite",
  "version": "0.1.0",
  "private": true,
  "description": "Reference SQLite-backed WOP host. Durable single-process implementation. NOT FOR PRODUCTION — used for conformance demo + 'build your own host' walkthrough demonstrating event-log persistence + claim acquisition.",
  "type": "module",
  "scripts": {
    "start": "tsx src/server.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "better-sqlite3": "^11.3.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.0",
    "@types/node": "^20.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
