{
  "name": "@wop/conformance",
  "version": "1.6.0",
  "private": true,
  "description": "Black-box conformance test suite for WOP-compliant servers. FINAL v1.0 (suite at 1.6.0) — first npm publication tracked as G10 phase 2 (see PUBLISHING.md).",
  "license": "Apache-2.0",
  "type": "module",
  "bin": {
    "wop-conformance": "./dist/cli.js"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "build:cli": "tsc",
    "cli": "tsx src/cli.ts"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.0.0",
    "@types/node": "^22.0.0",
    "ajv": "^8.17.0",
    "ajv-formats": "^3.0.1"
  }
}
